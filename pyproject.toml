[project]
name = "backend-workflow-automation"
version = "0.1.0"
description = "Sanitized portfolio project for Python backend workflow automation and validation."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
