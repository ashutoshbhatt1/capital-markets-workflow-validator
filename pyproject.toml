[project]
name = "capital-markets-workflow-validator"
version = "0.2.0"
description = "Read-only GPT-5.6 Sol incident explainer for deterministic workflow controls."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
